# Legacy agent role stubs (read-only)

`coder/`, `executor/`, `planner/`, `reviewer/`, `verifier/` (~870-byte
`__init__` stubs each, zero importers). Live agent code is
`src/agents/implementations.py`, `executive_agent.py`, `meta_factory.py`
plus `src/hermes_os/agent_fabric.py` and `HermesController` delegation.
