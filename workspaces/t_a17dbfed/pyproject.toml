[project]
name = "ai-governance-ethics"
version = "0.1.0"
description = "AI Governance & Ethics Framework"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
