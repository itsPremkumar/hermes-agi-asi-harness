apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: contextvault-data
  namespace: contextvault
  labels:
    app: contextvault
    component: memory-store
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 10Gi
