# Hermes AGI/ASI Harness — Python dependencies
# Runtime deps mirror [project.dependencies] in pyproject.toml (single source of truth there).
# This file exists for `pip install -r requirements.txt` workflows.
# All dependencies are MIT/Apache-2.0/BSD licensed.

# Core runtime
pyyaml>=6.0
psutil>=5.9.0
aiofiles>=25.1.0

# HTTP / Web
httpx>=0.27.0
beautifulsoup4>=4.15.0
trafilatura>=1.12.0
ddgs>=9.0.0

# Dev / test
pytest>=7.4.0
pytest-asyncio>=0.23.0
ruff>=0.4.0

# Type checking (optional)
mypy>=1.5.0
