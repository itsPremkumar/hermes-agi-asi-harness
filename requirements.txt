# Hermes AGI/ASI Harness — Python dependencies
# Mirrors [project.dependencies] in pyproject.toml — keep the two in sync.
# (pyproject.toml is the single source of truth; this file exists for
# `pip install -r requirements.txt` workflows.)
# All dependencies are MIT/Apache-2.0/BSD licensed.

# Core runtime (== [project.dependencies])
langgraph>=1.0.0
langchain-core>=0.3.0
pyyaml>=6.0.3
psutil>=5.9.0
aiofiles>=23.2.0

# HTTP / Web
httpx>=0.27.0
beautifulsoup4>=4.12.0
trafilatura>=2.2.0
ddgs>=9.0.0

# API dashboard (optional, == [project.optional-dependencies] api)
# pip install -e ".[api]"
# fastapi>=0.110.0
# uvicorn>=0.29.0

# MCP stdio server (optional, == [project.optional-dependencies] mcp)
# Lets Hermes Agent call harness tools as native MCP tools.
# pip install -e ".[mcp]"
# mcp>=2.0

# Dev / test
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.0
ruff>=0.16.5

# Type checking (optional)
mypy>=1.5.0
